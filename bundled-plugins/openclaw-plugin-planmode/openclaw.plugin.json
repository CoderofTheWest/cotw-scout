{
  "id": "planmode",
  "activation": {
    "onStartup": true
  },
  "name": "Plan Mode",
  "description": "Lightweight plan mode — detects plan_complete blocks, persists plans, handles approval/rejection, injects active plan context",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean", "default": true }
    }
  },
  "contracts": {
    "tools": [
      "plan_status"
    ]
  }
}
