{
  "name": "cotw-telemetry-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker for COTW beta telemetry ingestion",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
